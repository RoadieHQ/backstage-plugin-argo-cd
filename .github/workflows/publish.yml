name: npm-publish
on:
  push:
    branches:
      - main
jobs:
  npm-publish:
    name: npm-publish
    runs-on: ubuntu-latest
    steps:
      - name: Use composite action backstage-plugin-publish-action
        uses: RoadieHQ/backstage-plugin-publish-action@main
